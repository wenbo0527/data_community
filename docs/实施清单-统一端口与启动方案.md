# 实施清单 - 统一端口与启动方案

**版本**: v1.0  
**日期**: 2026-04-14  
**负责人**: 钟离  

---

## 📋 实施前准备

### 确认文件已创建 ✅

| 文件 | 路径 | 状态 |
|:---|:---|:---:|
| package.json | `~/Documents/project/data_community/package.json` | ✅ 已创建 |
| .env | `~/Documents/project/data_community/.env` | ✅ 已创建 |
| start-all.js | `~/Documents/project/data_community/scripts/start-all.js` | ✅ 已创建 |

### 待创建文件 ⏳

| 文件 | 路径 | 说明 |
|:---|:---|:---|
| check-ports.js | `scripts/check-ports.js` | 端口占用检查脚本 |
| kill-all.js | `scripts/kill-all.js` | 一键停止脚本 |

---

## 🔧 实施步骤

### Step 1: 创建缺失的脚本文件 (5分钟)

#### 1.1 创建 check-ports.js

```javascript
#!/usr/bin/env node
/**
 * 端口占用检查脚本
 */

const { exec } = require('child_process');

const ports = [8000, 8001, 8002, 8003, 8004, 8005, 3001];

console.log('🔍 检查端口占用情况...\n');

ports.forEach(port => {
  exec(`lsof -i :${port} | grep LISTEN`, (err, stdout) => {
    if (stdout) {
      console.log(`❌ 端口 ${port}: 已被占用`);
      console.log(`   ${stdout.trim()}`);
    } else {
      console.log(`✅ 端口 ${port}: 可用`);
    }
  });
});
```

#### 1.2 创建 kill-all.js

```javascript
#!/usr/bin/env node
/**
 * 一键停止所有服务脚本
 */

const { exec } = require('child_process');

const ports = [8000, 8001, 8002, 8003, 8004, 8005, 3001];

console.log('🛑 正在停止所有服务...\n');

ports.forEach(port => {
  exec(`lsof -ti :${port} | xargs kill -9 2>/dev/null || true`, (err) => {
    if (!err) {
      console.log(`✅ 端口 ${port}: 已释放`);
    }
  });
});

setTimeout(() => {
  console.log('\n👋 所有服务已停止');
  process.exit(0);
}, 2000);
```

创建这两个文件：

```bash
cd ~/Documents/project/data_community

# 创建 check-ports.js
cat > scripts/check-ports.js << 'EOF'
#!/usr/bin/env node
/**
 * 端口占用检查脚本
 */

const { exec } = require('child_process');

const ports = [8000, 8001, 8002, 8003, 8004, 8005, 3001];

console.log('🔍 检查端口占用情况...\n');

ports.forEach(port => {
  exec(`lsof -i :${port} | grep LISTEN`, (err, stdout) => {
    if (stdout) {
      console.log(`❌ 端口 ${port}: 已被占用`);
      console.log(`   ${stdout.trim()}`);
    } else {
      console.log(`✅ 端口 ${port}: 可用`);
    }
  });
});
EOF

# 创建 kill-all.js
cat > scripts/kill-all.js << 'EOF'
#!/usr/bin/env node
/**
 * 一键停止所有服务脚本
 */

const { exec } = require('child_process');

const ports = [8000, 8001, 8002, 8003, 8004, 8005, 3001];

console.log('🛑 正在停止所有服务...\n');

ports.forEach(port => {
  exec(`lsof -ti :${port} | xargs kill -9 2>/dev/null || true`, (err) => {
    if (!err) {
      console.log(`✅ 端口 ${port}: 已释放`);
    }
  });
});

setTimeout(() => {
  console.log('\n👋 所有服务已停止');
  process.exit(0);
}, 2000);
EOF

# 添加执行权限
chmod +x scripts/check-ports.js scripts/kill-all.js

echo "✅ 脚本文件创建完成"
```

---

## Step 2: 修改各子应用的端口配置

### 2.1 risk-app (8001)

修改 `apps/risk-app/vite.config.ts`：

```typescript
export default defineConfig({
  base: '/risk/',
  server: {
    port: 8001,  // 从5176改为8001
    host: '0.0.0.0',
  },
  // ...其他配置
})
```

### 2.2 mkt-app (8002)

修改 `apps/mkt-app/vite.config.ts`：

```typescript
export default defineConfig({
  base: '/mkt/',
  server: {
    port: 8002,  // 从5177改为8002
    host: '0.0.0.0',
  },
  // ...其他配置
})
```

### 2.3 dex-app (8003)

修改 `apps/dex-app/vite.config.ts`：

```typescript
export default defineConfig({
  base: '/dex/',
  server: {
    port: 8003,  // 从5180改为8003
    host: '0.0.0.0',
  },
  // ...其他配置
})
```

### 2.4 dmt-app (8004)

修改 `apps/dmt-app/vite.config.ts`：

```typescript
export default defineConfig({
  base: '/dmt/',
  server: {
    port: 8004,  // 从5184改为8004
    host: '0.0.0.0',
  },
  // ...其他配置
})
```

### 2.5 admin-app (8005)

修改 `apps/admin-app/vite.config.ts`：

```typescript
export default defineConfig({
  base: '/admin/',
  server: {
    port: 8005,  // 从5182改为8005
    host: '0.0.0.0',
  },
  // ...其他配置
})
```

执行命令批量修改：

```bash
cd ~/Documents/project/data_community

# 修改risk-app端口
sed -i '' 's/port: 5176/port: 8001/' apps/risk-app/vite.config.ts

# 修改mkt-app端口
sed -i '' 's/port: 5177/port: 8002/' apps/mkt-app/vite.config.ts

# 修改dex-app端口
sed -i '' 's/port: 5180/port: 8003/' apps/dex-app/vite.config.ts

# 修改dmt-app端口
sed -i '' 's/port: 5184/port: 8004/' apps/dmt-app/vite.config.ts

# 修改admin-app端口
sed -i '' 's/port: 5182/port: 8005/' apps/admin-app/vite.config.ts

echo "✅ 所有子应用端口修改完成"
```

---

## Step 3: 更新portal-shell注册配置

修改 `portal-shell/src/App.vue` 中的子应用注册：

```typescript
const apps: AppConfig[] = [
  { 
    name: '数字风险域 (RISK)', 
    entry: 'http://localhost:8001/',  // 从5176改为8001
    // ...其他配置
  },
  { 
    name: '营销域 (MKT)', 
    entry: 'http://localhost:8002/',  // 从5177改为8002
    // ...其他配置
  },
  { 
    name: '数据探索域 (DEX)', 
    entry: 'http://localhost:8003/',  // 从5180改为8003
    // ...其他配置
  },
  { 
    name: '数据管理域 (DMT)', 
    entry: 'http://localhost:8004/',  // 从5184改为8004
    // ...其他配置
  },
  { 
    name: '通用域 (ADMIN)', 
    entry: 'http://localhost:8005/',  // 从5182改为8005
    // ...其他配置
  },
  // ...其他配置
]
```

执行修改：

```bash
# 修改portal-shell的App.vue
cd ~/Documents/project/portal-shell

sed -i '' 's/localhost:5176/localhost:8001/' src/App.vue
sed -i '' 's/localhost:5177/localhost:8002/' src/App.vue
sed -i '' 's/localhost:5180/localhost:8003/' src/App.vue
sed -i '' 's/localhost:5184/localhost:8004/' src/App.vue
sed -i '' 's/localhost:5182/localhost:8005/' src/App.vue

echo "✅ portal-shell端口配置更新完成"
```

---

## Step 4: 安装依赖并验证

### 4.1 安装根依赖

```bash
cd ~/Documents/project/data_community

# 安装concurrently等依赖
npm install

echo "✅ 根依赖安装完成"
```

### 4.2 验证所有配置

```bash
# 检查端口占用
npm run check:ports

# 预期输出：
# ✅ 端口 8000: 可用
# ✅ 端口 8001: 可用
# ✅ 端口 8002: 可用
# ✅ 端口 8003: 可用
# ✅ 端口 8004: 可用
# ✅ 端口 8005: 可用
# ✅ 端口 3001: 可用
```

### 4.3 一键启动测试

```bash
# 启动所有服务
npm run dev:all

# 等待所有服务启动完成（约30秒）

# 访问主框架
open http://localhost:8000

# 测试各子应用切换
# 验证6个子应用都能正常加载
```

### 4.4 验证检查表

| 检查项 | 预期结果 | 状态 |
|:---|:---|:---:|
| 8000端口可访问 | portal-shell首页 | ⬜ |
| 8001端口可访问 | RISK首页 | ⬜ |
| 8002端口可访问 | MKT首页 | ⬜ |
| 8003端口可访问 | DEX首页 | ⬜ |
| 8004端口可访问 | DMT首页 | ⬜ |
| 8005端口可访问 | ADMIN首页 | ⬜ |
| portal-shell子应用切换 | 6个应用均可加载 | ⬜ |
| **全部通过** | | ⬜ |

---

## ✅ 实施完成确认

当以上所有步骤执行完毕，并且验证检查表全部通过后，说明统一端口规划与启动方案实施完成。

**确认签名：**

- [ ] 步骤1完成 - 脚本文件创建
- [ ] 步骤2完成 - 子应用端口修改
- [ ] 步骤3完成 - portal-shell配置更新
- [ ] 步骤4完成 - 依赖安装与验证
- [ ] **全部验证通过** - 方案实施完成

实施完成日期：_____________
实施人签名：_____________

---

**🏛️ 契约已定，待实施部署。**

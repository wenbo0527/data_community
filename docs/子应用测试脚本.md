/**
 * 子应用点击测试脚本
 * 用于自动化测试各子应用是否可以正常加载
 */

// 测试步骤说明

// 1. 先启动所有子应用
const subApps = [
  { name: 'mkt-app', port: 5177, route: '/mkt/' },
  { name: 'admin-app', port: 5182, route: '/admin/' },
  { name: 'asset-app', port: 5179, route: '/asset/' },
  { name: 'dex-app', port: 5180, route: '/dex/' },
  { name: 'dmt-app', port: 5181, route: '/dmt/' },
  { name: 'risk-app', port: 5176, route: '/risk/' }
]

// 2. 使用 Playwright 或 Puppeteer 进行自动化测试

/*
import { chromium } from 'playwright'

async function testAllApps() {
  const browser = await chromium.launch()
  const context = await browser.newContext()
  
  const results = []
  
  for (const app of subApps) {
    console.log(`测试 ${app.name}...`)
    
    const page = await context.newPage()
    
    // 监听控制台错误
    const errors = []
    page.on('console', msg => {
      if (msg.type() === 'error') {
        errors.push(msg.text())
      }
    })
    
    // 监听页面错误
    page.on('pageerror', err => {
      errors.push(err.message)
    })
    
    try {
      await page.goto(`http://localhost:${app.port}${app.route}`, { timeout: 10000 })
      await page.waitForTimeout(2000) // 等待页面加载
      
      results.push({
        app: app.name,
        status: errors.length === 0 ? '✅ 通过' : '❌ 失败',
        errors: errors.length > 0 ? errors : '无'
      })
    } catch (e) {
      results.push({
        app: app.name,
        status: '❌ 加载失败',
        error: e.message
      })
    }
    
    await page.close()
  }
  
  console.table(results)
  
  await browser.close()
}

testAllApps()
*/

// 3. 手动测试清单

/*
请在浏览器中按以下步骤测试:

1. 打开 http://localhost:4100/home/ (门户)
2. 依次点击以下应用卡片:
   - 营销域 (MKT) -> http://localhost:5177/mkt/
   - 通用域 (COM) -> http://localhost:5182/admin/
   - 数据资产域 (DFD) -> http://localhost:5179/asset/
   - 数据探索域 (DEX) -> http://localhost:5180/dex/
   - 数据管理域 (DMT) -> http://localhost:5181/dmt/
   - 数字风险域 (RISK) -> http://localhost:5176/risk/

3. 检查每个页面的:
   - 控制台是否有红色错误 (Error)
   - 页面是否正常显示内容
   - 是否有空白页面或加载失败

4. 记录发现的问题
*/

console.log('请使用浏览器手动测试，或运行 Playwright 自动化脚本')
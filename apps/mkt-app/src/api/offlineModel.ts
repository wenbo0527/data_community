export const offlineModelApi = {
  getFeatureRows: async () => [],
  getOfflineModels: async () => []
}

export const featureAPI = {
  getFeatures: async () => [],
  getFeatureDetail: async (id: string) => ({ id })
}

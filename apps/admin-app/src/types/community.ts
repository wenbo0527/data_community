export interface CommunityType {
  id: string
  name: string
}

export interface ResourceCategory {
  value: string
  label: string
}

export const DATA_ASSET_OPTIONS: { value: string; label: string }[] = []

## 目标
- 基础信息固定在页面上方（标题下方），不随画布滚动；横版画布范围在其下方。
- 工具条保留在画布容器内，以覆盖方式置于画布顶部，不进入页面基础信息区域。

## 修改方案
### 模板结构
1. 将 `<a-card class="basic-info-card">` 移至 `page-header` 下方、`content` 之外，固定在页面上方。
   - 参考位置：`src/pages/marketing/tasks/horizontal/index.vue:8–47`
2. 保留 `CanvasToolbar` 于画布容器内：移动到 `.canvas-container` 内部，作为绝对定位覆盖层，放在最顶部。
   - 参考位置：`src/pages/marketing/tasks/horizontal/index.vue:82–90`（在 `canvasContainerRef` 容器内插入工具条）

### 样式
- 为工具条增加覆盖样式（示例）：`.canvas-toolbar-overlay { position: absolute; top: 8px; left: 12px; z-index: 5; }`，确保位于画布之上但不影响基础信息卡片。
- `content` 保持 `flex: 1; overflow: hidden;`；`.basic-info-card` 按现有边距样式保留（`index.vue:3251–3259`）。
- 确保 `NodeTypeSelector`、`selector-backdrop`、`minimap` 与节点/边菜单的 `z-index` 低于工具条覆盖层，避免遮挡。

### 行为与兼容
- 工具条事件逻辑不变：快速布局、缩放、历史（含撤销/重做）、添加节点。
- `handleAddNode` 继续使用 `contentRef` 计算相对定位；若绝对覆盖工具条需要锚点矩形，使用工具条自身 `getBoundingClientRect()` 与 `contentRef` 的差值计算。
- 保存/发布逻辑不变，仍使用基础信息字段与序列化的 `canvasData`。

## 验证
- 画布滚动时，基础信息区域固定在上方；工具条覆盖在画布内顶部，不进入基础信息区域。
- 添加节点时选择器出现于画布区域内；快速布局、缩放、历史操作均正常。
- 保存/发布后返回列表页，记录正确显示。

## 影响范围
- 仅 `src/pages/marketing/tasks/horizontal/index.vue` 的模板结构与样式少量调整；不影响路由与存储模块。

确认后我将按此方案实施并进行预览验证。
## 目标
- 在“首页”（预算总览页）删除筛选与预算上传模块，只保留功能导航与关键指标区，以及预算监控图与跳转按钮。

## 改动范围
- 文件：`src/pages/budget/BudgetOverview.vue`
- 模板与脚本的筛选、上传相关代码与依赖。

## 实施步骤
### 模板调整
- 删除“筛选与操作工具栏”整块：`src/pages/budget/BudgetOverview.vue:17-53`（`<a-card class="toolbar">` 内的 `<a-form>` 包含“业务类型/平台产品/年份/查询/重置/上传预算数据/新增预算”）。
- 保留并置顶“功能导航”卡片：跳转到列表/监控/合同/结算。
- 保留“KPI四项”与“预算监控”卡片；监控卡片右上保留“前往预算监控”按钮。

### 脚本清理
- 移除筛选与上传相关变量/方法（若仍残留）：`filterForm`、`platformOptions`、`yearOptions`（模板不再使用时可保留或移除）、`handleFilter`、`resetFilter`、`handleUpload`、`openCreateBudget`。
- 移除未使用的导入：`IconUpload`、`PlatformProductModal`、与上传/新增相关类型引用。
- 已保留的内容：`useBudgetStatsAggregator/useBudgetCalculations`、`BudgetBurndownTabs`、`useExternalDataStore`、`useContractStore`。

### 样式清理
- 删除或保留`.toolbar { margin-bottom: 12px; }`（若不再使用则移除）。

### 验证
- 运行类型检查：`npm run type-check:fast`，确保移除后无未定义引用。
- 打开页面：`/risk/budget-overview`，确认：
  - 页头下仅显示“功能导航”卡片。
  - KPI显示“当年预算总额/当前累积消耗/当年实际核销/预算健康度”。
  - 预算监控图可切换粒度与模式，并可跳转到监控页。
  - 页面无筛选表单与“上传预算/新增预算”按钮。

## 不影响内容
- 保留导航、KPI、燃尽/累积监控图与跳转按钮。
- 不改动其他路由页面（列表/监控/合同/结算）。

## 回滚策略
- 若影响展示或报错，回滚删除的模板块与对应脚本引用，逐项恢复。
# 三大模块代码整合方案 v1.0

> **版本**: v1.0
> **日期**: 2026-08-02
> **前置**: 死代码清理已落地(882 行死代码已删 + 1 个隐藏 bug 已修)
> **目标**: 把"发现/管理/探索"三个模块在**数据层、路由层、表现层**做最小侵入式整合,共用同一套底座

---

## 0 · 整合的 4 个层面

```
┌──────────────────────────────────────────────────────┐
│  L4 表现层 · 统一工作台(替换 discovery/management 占位)│   ← 用户能看见的变化
├──────────────────────────────────────────────────────┤
│  L3 路由层 · 跨模块跳转契约 + 统一面包屑               │   ← 不再有硬编码 path
├──────────────────────────────────────────────────────┤
│  L2 API 层 · HTTP 契约标准化(对接 shared/search-api)  │   ← 跨域搜索就绪
├──────────────────────────────────────────────────────┤
│  L1 数据层 · 共享 Store 真接入(MetadataStore 等)       │   ← 真整合的根
└──────────────────────────────────────────────────────┘
```

**核心原则**: **自下而上** 改,每一层完成后立刻可见收益。

---

## 1 · L1 数据层整合(根基)

### 1.1 现状问题
- `MetadataStore` / `BusinessConceptStore` / `StandardStore` 三个 shared Store 已存在
- 但 **只有 management 用了**(modeling / standards / business-concept × 3)
- discovery/exploration 仍然用本地 mock 常量

### 1.2 整合动作:把 discovery 全套"数据地图"切到 MetadataStore

#### 文件 1: [src/pages/discovery/data-map/TableList.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/discovery/data-map/TableList.vue)

**当前**:
```ts
import { tableMockData } from '@/mock/tableData.ts'
import { mockDataAssets } from '@/mock/data-map.ts'
```

**改为**:
```ts
import { MetadataStore } from '@/mock/shared/metadata-store'

// 替换 loadData
const tables = computed(() => MetadataStore.getTables({ domain: filterForm.domain }))
```

#### 文件 2: [src/pages/discovery/data-map/index.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/discovery/data-map/index.vue)

**当前**: 多处 `import ... from '@/mock/data-map.ts'`
**改为**: 全部走 `MetadataStore`(表 / 字段 / 血缘 / 收藏夹)

#### 文件 3: [src/pages/discovery/customer360/index.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/discovery/customer360/index.vue)

**当前**:
```ts
import { mockUsers, fetchUserInfo } from '../../../mock/customer360'
```

**改为**(只动引用方式,不破坏数据结构):
```ts
import { fetchUserInfo } from '@/mock/customer360'
import { useCustomerStore } from '@/stores/customer360'  // 新建 Pinia store
const customerStore = useCustomerStore()
const users = computed(() => customerStore.list)
```

> 说明:`customer360.ts` 本身不需要改,只需在它外面包一个 Pinia store,把 `mockUsers` 暴露成响应式。

#### 文件 4: [src/pages/discovery/data-map/TableDetailPage.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/discovery/data-map/TableDetailPage.vue)

已经部分接 MetadataStore,只需补齐字段级 API 缺失。

### 1.3 新增 Pinia Store(整合粘合层)

| Store | 位置 | 用途 |
|:---|:---|:---|
| `useCustomerStore` | `src/stores/customer360.ts` | 包装 `mockUsers`,响应式 |
| `useMetricStore` | `src/stores/metrics.ts` | 包装 `metrics.ts`,响应式 |
| `useVariableStore` | 已存在 | 扩展 |

> Pinia 比直接 `import` mock 强在:跨页面共享 + 响应式 + 可被 watch。

### 1.4 L1 验收标准
- `npm run typecheck` 通过
- `discovery/data-map/*` 路由下数据展示与改造前一致
- `customer360/*` 路由下数据展示与改造前一致

---

## 2 · L2 API 层整合(接 search-api)

### 2.1 现状
- `@/api/community` 的 `searchApi` 已接 shared/search-api,**只被 discovery/search 用**
- exploration 顶部已经有 `<a-input-search>`,但**没有接后端**

### 2.2 整合动作:exploration 工作台接 search-api

#### 文件: [src/pages/exploration/index.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/exploration/index.vue)

**当前**(line 10-23):
```vue
<a-input-search
  placeholder="搜索表、指标、报表或仪表盘..."
  size="large"
  button-text="搜索"
  search-button
  class="global-search"
/>
```

**改为**:
```vue
<a-input-search
  v-model="searchKeyword"
  placeholder="搜索表/指标/标签/概念..."
  size="large"
  search-button
  @search="handleSearch"
  @press-enter="handleSearch"
  class="global-search"
/>

<!-- 搜索结果抽屉 -->
<a-drawer v-model:visible="searchDrawer" title="跨模块搜索结果" :width="720">
  <GlobalSearchResult :keyword="lastKeyword" @navigate="onNav" />
</a-drawer>
```

**script 部分**:
```ts
import { searchApi } from '@/api/community'

const searchKeyword = ref('')
const searchDrawer = ref(false)
const lastKeyword = ref('')

const handleSearch = async () => {
  if (!searchKeyword.value.trim()) return
  lastKeyword.value = searchKeyword.value
  searchDrawer.value = true
}

const onNav = (result: any) => {
  // 跨模块跳转,见 L3
  router.push(result.routePath)
}
```

### 2.3 新增组件

#### `src/components/common/GlobalSearchResult.vue`

```vue
<template>
  <div class="global-search-result">
    <a-tabs v-model:active-key="activeTab">
      <a-tab-pane key="table" title="数据表">
        <ResultList :items="results.tables" module="discovery" sub="data-map" />
      </a-tab-pane>
      <a-tab-pane key="metric" title="指标">
        <ResultList :items="results.metrics" module="discovery" sub="metrics-map" />
      </a-tab-pane>
      <a-tab-pane key="tag" title="标签">
        <ResultList :items="results.tags" module="exploration" sub="tag-system" />
      </a-tab-pane>
      <a-tab-pane key="concept" title="业务概念">
        <ResultList :items="results.concepts" module="management" sub="business-concept" />
      </a-tab-pane>
    </a-tabs>
  </div>
</template>

<script setup lang="ts">
import { ref, watch } from 'vue'
import { searchApi } from '@/api/community'

const props = defineProps<{ keyword: string }>()
const emit = defineEmits<{ navigate: [result: any] }>()

const activeTab = ref('table')
const results = ref({ tables: [], metrics: [], tags: [], concepts: [] })

watch(() => props.keyword, async (kw) => {
  if (!kw) return
  const res: any = await searchApi({ keyword: kw, include: 'all' })
  results.value = {
    tables: res.data?.tables || [],
    metrics: res.data?.concepts?.elements || [],
    tags: [],  // 暂未在 search-api 中
    concepts: res.data?.concepts || {}
  }
}, { immediate: true })
</script>
```

### 2.4 L2 验收标准
- exploration 工作台输入关键词能搜出表/指标/概念
- 点击结果跨模块跳转(由 L3 保证)

---

## 3 · L3 路由层整合(契约化跳转)

### 3.1 现状问题
[exploration/index.vue:232-235](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/exploration/index.vue#L232-L235) 直接:
```ts
const goDiscovery = () => router.push('/discovery/index')
const goMarketing = () => router.push('/marketing/index')
const goExternal = () => router.push('/external-data-v1/index')
const goManagement = () => router.push('/management/index')
```

**风险**: 路由路径改了不知道,字符串散落各处。

### 3.2 整合动作:新增 `useCrossNav` composable

#### 文件: [src/composables/useCrossNav.ts](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/composables/useCrossNav.ts) (新建)

```ts
import { useRouter } from 'vue-router'

/**
 * 跨模块/同模块导航契约
 * 替代散落的 router.push('/xxx/yyy') 字符串
 */
export const ROUTE_KEYS = {
  // discovery
  discoveryHome: { module: 'discovery', page: 'index' },
  discoveryDataMap: { module: 'discovery', page: 'data-map' },
  discoveryCustomer360: { module: 'discovery', page: 'customer360' },
  discoveryMetrics: { module: 'discovery', page: 'metrics-map' },
  discoveryLineage: { module: 'discovery', page: 'lineage' },
  // management
  managementHome: { module: 'management', page: 'service' },
  managementMetadata: { module: 'management', page: 'metadata/modeling' },
  managementStandard: { module: 'management', page: 'data-standard/standards' },
  managementDataPermission: { module: 'management', page: 'permission/data-permission' },
  // exploration
  explorationHome: { module: 'exploration', page: 'index' },
  explorationTags: { module: 'exploration', page: 'customer-center/tag-system' },
  explorationEvents: { module: 'exploration', page: 'customer-center/event-center' },
  explorationAudience: { module: 'exploration', page: 'customer-center/audience-system/audience-management' },
  explorationWorkflows: { module: 'exploration', page: 'workflows' },
  explorationDashboard: { module: 'exploration', page: 'indicator-dashboard' },
} as const

const ROUTE_TABLE: Record<string, string> = {
  // discovery
  'discovery:index': '/discovery/index',
  'discovery:data-map': '/discovery/data-map',
  'discovery:customer360': '/discovery/customer360',
  'discovery:customer360-detail': '/discovery/customer360/detail',  // + /:userId
  'discovery:metrics-map': '/discovery/metrics-map',
  'discovery:lineage': '/discovery/lineage',
  // management
  'management:index': '/management/index',
  'management:service': '/management/service',
  'management:metadata-modeling': '/management/metadata/modeling',
  'management:data-permission': '/management/permission/data-permission',
  'management:business-concept': '/management/business-concept',
  // exploration
  'exploration:index': '/exploration/index',
  'exploration:tag-system': '/exploration/customer-center/tag-system',
  'exploration:event-center': '/exploration/customer-center/event-center',
  'exploration:audience-management': '/exploration/customer-center/audience-system/audience-management',
  'exploration:workflows': '/exploration/workflows',
  'exploration:indicator-dashboard': '/exploration/indicator-dashboard',
}

export function useCrossNav() {
  const router = useRouter()

  const go = (key: keyof typeof ROUTE_TABLE, params?: Record<string, any>) => {
    let path = ROUTE_TABLE[key]
    if (params) {
      Object.entries(params).forEach(([k, v]) => {
        path = path.replace(`:${k}`, String(v))
      })
    }
    return router.push(path)
  }

  /** 兼容旧菜单配置:从 menuConfig 里的 path 字段解析 */
  const goFromMenuPath = (moduleKey: string, pageKey: string) => {
    return go(`${moduleKey}:${pageKey}` as any)
  }

  return { go, goFromMenuPath, ROUTE_KEYS }
}
```

### 3.3 替换散落的硬编码

#### [src/pages/exploration/index.vue:232-235](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/exploration/index.vue#L232-L235)

```diff
- const goDiscovery = () => router.push('/discovery/index')
- const goMarketing = () => router.push('/marketing/index')
- const goExternal = () => router.push('/external-data-v1/index')
- const goManagement = () => router.push('/management/index')
+ const { go } = useCrossNav()
+ const goDiscovery = () => go('discovery:index')
+ const goMarketing = () => go('marketing:index')
+ const goExternal = () => go('external:data-v1')
+ const goManagement = () => go('management:index')
```

### 3.4 L3 验收标准
- 全仓 `grep "router.push('/" src/` 命中数 < 10 处(目前 30+ 处)
- 改动后 navigation 仍正常工作

---

## 4 · L4 表现层整合(统一工作台)

### 4.1 现状
- exploration 工作台已有完整范式:[exploration/index.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/exploration/index.vue)
- discovery/management 入口都是占位空壳
- 这正是整合切入点

### 4.2 整合动作:抽出 `<UnifiedWorkbench>` 组件,三模块共用

#### 文件 1: `src/components/common/UnifiedWorkbench.vue`(新建)

```vue
<template>
  <div class="unified-workbench">
    <!-- 1. Hero 区(全局搜索 + 问候) -->
    <section class="hero">
      <h1>{{ greeting }}</h1>
      <GlobalSearch />
    </section>

    <!-- 2. 当前模块快捷作业(由 props.module 决定数据) -->
    <section class="shortcuts">
      <h2>{{ moduleTitle }} · 快捷作业</h2>
      <WorkbenchCardGrid :tools="shortcuts" @click="onToolClick" />
    </section>

    <!-- 3. 最近访问(从 localStorage 读) -->
    <section class="recent">
      <h2>最近访问</h2>
      <RecentVisitList :visits="recentVisits" />
    </section>

    <!-- 4. 跨模块跳转入口(发现/管理/探索 三入口卡片) -->
    <section class="cross-nav">
      <h2>进入其他模块</h2>
      <CrossModuleCard :modules="['discovery', 'management', 'exploration']" />
    </section>

    <!-- 5. 收藏 + 快捷报表(可选) -->
    <section class="quick-reports">
      <h2>快捷报表</h2>
      <QuickReportGrid :reports="quickReports" />
    </section>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { useCrossNav } from '@/composables/useCrossNav'

const props = defineProps<{
  module: 'discovery' | 'management' | 'exploration'
  greeting?: string
  shortcuts: Array<{ key: string; title: string; icon: any; desc: string; navKey: string }>
}>()

const route = useRoute()
const { go } = useCrossNav()

const moduleTitle = computed(() => ({
  discovery: '数据发现',
  management: '数据管理',
  exploration: '数据探索',
})[props.module])

const greeting = computed(() => props.greeting || '欢迎使用数据社区')

const recentVisits = ref<any[]>([])
const quickReports = ref<any[]>([])

const onToolClick = (tool: any) => go(tool.navKey as any)

onMounted(() => {
  // 从 localStorage 读最近访问 / 收藏
  recentVisits.value = JSON.parse(localStorage.getItem('recent-visits') || '[]')
})
</script>
```

#### 文件 2-4: 替换三个入口

##### [src/pages/discovery/index.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/discovery/index.vue)

```vue
<template>
  <UnifiedWorkbench
    module="discovery"
    :shortcuts="discoveryShortcuts"
  />
</template>

<script setup lang="ts">
import UnifiedWorkbench from '@/components/common/UnifiedWorkbench.vue'
import { IconSearch, IconStorage, IconUserGroup, IconBranch } from '@arco-design/web-vue/es/icon'

const discoveryShortcuts = [
  { key: 'data-map', title: '数据地图', icon: IconStorage, desc: '浏览所有数据资产', navKey: 'discovery:data-map' },
  { key: 'customer360', title: '客户 360', icon: IconUserGroup, desc: '客户全景查询', navKey: 'discovery:customer360' },
  { key: 'metrics-map', title: '指标地图', icon: IconBranch, desc: '业务指标体系', navKey: 'discovery:metrics-map' },
  { key: 'search', title: '统一搜索', icon: IconSearch, desc: '跨域搜索', navKey: 'discovery:data-map' },
]
</script>
```

##### [src/pages/management/index.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/management/index.vue)

```vue
<template>
  <UnifiedWorkbench
    module="management"
    :shortcuts="managementShortcuts"
  />
</template>

<script setup lang="ts">
import UnifiedWorkbench from '@/components/common/UnifiedWorkbench.vue'
import { IconSettings, IconSafe, IconStorage, IconCommon } from '@arco-design/web-vue/es/icon'

const managementShortcuts = [
  { key: 'metadata', title: '元数据建模', icon: IconStorage, desc: '管理数据资产元数据', navKey: 'management:metadata-modeling' },
  { key: 'data-permission', title: '数据权限', icon: IconSafe, desc: '字段级权限配置', navKey: 'management:data-permission' },
  { key: 'standard', title: '数据标准', icon: IconCommon, desc: '业务标准治理', navKey: 'management:data-permission' },
  { key: 'service', title: '数据服务', icon: IconSettings, desc: 'API/模型管理', navKey: 'management:service' },
]
</script>
```

##### [src/pages/exploration/index.vue](file:///Users/mac/nis_mock/data_comunity/data_comunity/src/pages/exploration/index.vue)

把现有内联模板整体迁移到 `UnifiedWorkbench` 组件,只保留 module-specific 数据。

### 4.3 L4 验收标准
- `/discovery` `/management` `/exploration` 三个入口都是"统一工作台"风格
- 工作台内点击快捷作业能正确跳转
- 全局搜索(L2)能在三个入口都用

---

## 5 · 落地顺序与里程碑

### 5.1 推荐执行顺序(由易到难)

| Step | 内容 | 改动面 | 风险 | 预计改动 |
|:---:|:---|:---:|:---:|:---:|
| **①** | L4 抽 UnifiedWorkbench 组件(只动 3 个入口页) | 小 | 🟢 低 | ~250 行 |
| **②** | L3 新增 useCrossNav,替换 exploration/index.vue 硬编码 | 小 | 🟢 低 | ~30 行 |
| **③** | L2 exploration 工作台接 search-api + 新增 GlobalSearchResult | 中 | 🟡 中 | ~150 行 |
| **④** | L1 customer360 包 Pinia store,data-map 切 MetadataStore | 中 | 🟡 中 | ~100 行 |
| **⑤** | L1 metrics/variable/feature-map 全部接 shared Store | 大 | 🟠 中高 | ~200 行 |

### 5.2 每个 Step 完成后立刻可见效果

| Step | 用户能看到什么 |
|:---:|:---|
| ① | `/discovery` `/management` 入口不再是空白,有了工作台 |
| ② | 跳转契约化,以后改路由不会漏改 |
| ③ | exploration 顶部搜索框真正能搜,搜出表/指标/概念 |
| ④ | data-map 表格从 MetadataStore 读(可被 management 上架操作影响) |
| ⑤ | 指标/变量全模块统一底座 |

### 5.3 不在本次整合范围(独立 PRD/任务)

- ❌ 客户 360 v3.3 字段权限真接入(需独立 PRD v3.3)
- ❌ metrics 三处 schema 统一(独立任务)
- ❌ 外数四模块统一(独立任务)
- ❌ 业务概念图谱跨模块复用(独立任务)

---

## 6 · 风险与缓解

| 风险 | 触发场景 | 缓解措施 |
|:---|:---|:---|
| R1 `discovery:data-map` 路由组件 index.vue 重名冲突 | router name 重复 | 替换时同步检查 router/index.js 的 name |
| R2 MetadataStore 数据格式与 data-map 本地 mock 不一致 | 字段缺失/类型不同 | 先并行跑(灰度):保留旧 import,新逻辑走 store |
| R3 UnifiedWorkbench 与 exploration/index.vue 现有功能冲突 | 快捷作业/最近访问数据源 | 灰度:先并行,功能验证后再删旧代码 |
| R4 search-api 返回结果与各模块路由不匹配 | routePath 字段缺失 | 在 GlobalSearchResult 内做映射兜底 |

---

## 7 · 验收清单(全整合完成后)

- [ ] `npm run typecheck` 通过
- [ ] `npm run lint:check` 通过
- [ ] 三个模块入口都是 UnifiedWorkbench
- [ ] 三个入口的全局搜索都能用
- [ ] data-map 数据从 MetadataStore 读,management 上架后立刻可见
- [ ] 全仓 `grep "router.push('/" src/` 命中 < 10 处
- [ ] 客户 360 详情仍可访问 `/discovery/customer360/detail/:userId`

---

## 版本记录

| 版本 | 日期 | 修改内容 | 作者 |
|:---|:---|:---|:---|
| v1.0 | 2026-08-02 | 初版:4 层整合方案(数据/路由/API/表现) | Claude |